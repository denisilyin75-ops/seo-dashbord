# SCC API Reference

> Auto-generated by code-review-agent. Last updated: 2026-04-18 20:13 UTC.
> Source: `server/routes/*.js`. Mount points: `server/index.js`.
> Всего endpoints: **60** в 15 группах.

## Index

- [Actions](#actions) — 3 endpoints
- [Agents](#agents) — 5 endpoints
- [Ai](#ai) — 4 endpoints
- [Articles](#articles) — 10 endpoints
- [Blog](#blog) — 5 endpoints
- [Content-health](#content-health) — 5 endpoints
- [Daily](#daily) — 2 endpoints
- [Deploy](#deploy) — 3 endpoints
- [Imported](#imported) — 4 endpoints
- [Log](#log) — 1 endpoint
- [Metrics](#metrics) — 1 endpoint
- [Plan](#plan) — 6 endpoints
- [Portfolio](#portfolio) — 1 endpoint
- [Prefs](#prefs) — 3 endpoints
- [Sites](#sites) — 7 endpoints

## Actions

Mount: `/api` (from `server/routes/actions.js`)

| Method | Path | Description |
|---|---|---|
| `POST` | `/api/actions` | POST /api/actions Body: { action_type, source_type: 'imported_article'\|'article', source_ids: [id], params: {...} } Выполняется sync (5-30 сек). В Phase 3b перейдём на background-job с polling'ом. |
| `GET` | `/api/actions/:id` | GET /api/actions/:id — получить результат конкретного action |
| `GET` | `/api/actions` | GET /api/actions?source_type=&source_id=&limit= |

## Agents

Mount: `/api/agents` (from `server/routes/agents.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/agents/` | GET /api/agents — все агенты |
| `GET` | `/api/agents/:id` | GET /api/agents/:id — детали одного агента |
| `PUT` | `/api/agents/:id` | PUT /api/agents/:id — обновить config/enabled/schedule |
| `POST` | `/api/agents/:id/run` | POST /api/agents/:id/run — принудительный запуск |
| `GET` | `/api/agents/:id/runs` | GET /api/agents/:id/runs — история запусков |

## Ai

Mount: `/api/ai` (from `server/routes/ai.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/ai/credits` | GET /api/ai/credits — остаток кредитов у провайдера (OpenRouter) |
| `POST` | `/api/ai/command` | POST /api/ai/command — выполнить произвольную команду |
| `POST` | `/api/ai/ideas` | POST /api/ai/ideas |
| `POST` | `/api/ai/site-plan` | POST /api/ai/site-plan — AI-план для deploy wizard |

## Articles

Mount: `/api` (from `server/routes/articles.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/sites/:siteId/articles` | GET /api/sites/:siteId/articles Основной endpoint списка. Поддерживает search + filters + sort + pagination. Response shape: - Если НИ одного filter/search параметра → backward-compatible: просто масс |
| `GET` | `/api/articles/search` | GET /api/articles/search — cross-site поиск (без привязки к site_id). Полезно для merge workflow ("найти все наши обзоры по Magnifica"). |
| `POST` | `/api/sites/:siteId/articles` | POST /api/sites/:siteId/articles |
| `PUT` | `/api/articles/:id` | PUT /api/articles/:id |
| `GET` | `/api/articles/:id/meta` | GET /api/articles/:id/meta — подтянуть meta-поля из WP (для редактора) |
| `DELETE` | `/api/articles/:id` | DELETE /api/articles/:id |
| `POST` | `/api/articles/:id/sync-wp` | POST /api/articles/:id/sync-wp — pull актуальной версии из WP по wp_post_id, или push нашей версии (?direction=push) в WP. |
| `POST` | `/api/sites/:siteId/articles/sync-all` | POST /api/sites/:siteId/articles/sync-all — pull всех опубликованных постов сайта в локальную БД |
| `GET` | `/api/articles/:id/revisions` | GET /api/articles/:id/revisions — история изменений статьи |
| `POST` | `/api/articles/bulk` | POST /api/articles/bulk — массовые операции над статьями. Body: { article_ids: [...], action: 'archive'\|'delete'\|'tag_add'\|'tag_remove'\|'status', payload? } Action 'archive':   status='archived' ( |

## Blog

Mount: `/api/blog` (from `server/routes/blog.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/blog/` | GET /api/blog?limit=50&offset=0&tag=gamification |
| `GET` | `/api/blog/:id` | GET /api/blog/:id |
| `POST` | `/api/blog/` | POST /api/blog  body: {title, body, tags?, pinned?} |
| `PUT` | `/api/blog/:id` | PUT /api/blog/:id |
| `DELETE` | `/api/blog/:id` | DELETE /api/blog/:id |

## Content-health

Mount: `/api` (from `server/routes/content-health.js`)

| Method | Path | Description |
|---|---|---|
| `POST` | `/api/quality/analyze` | POST /api/quality/analyze Body: { site_id, post_id?, post_url (required — абсолютный URL или pathname) } Возвращает { score_overall, scores, issues, stats, run_id }. |
| `POST` | `/api/sites/:siteId/quality/batch` | POST /api/sites/:siteId/quality/batch?limit=N Запускает анализ N последних published статей. |
| `GET` | `/api/sites/:siteId/content-health` | GET /api/sites/:siteId/content-health Query: severity=red\|yellow\|green, category=seo_hygiene\|schema\|..., limit=N Response: { summary: {red,yellow,green}, avg_score, recent_analyzed, issues: [...]  |
| `PATCH` | `/api/content-health/:id` | PATCH /api/content-health/:id Body: { action: 'resolved' \| 'ignored', reason? } |
| `GET` | `/api/sites/:siteId/quality/scores` | GET /api/sites/:siteId/quality/scores?limit=N История оценок для trending-графика. |

## Daily

Mount: `/api/daily-brief` (from `server/routes/daily.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/daily-brief/` | GET /api/daily-brief?siteId=...&refresh=1 Возвращает brief для одного сайта. Без siteId — массив для всех active сайтов. |
| `GET` | `/api/daily-brief/ideas-history` | GET /api/daily-brief/ideas-history?siteId=...&limit=30 Возвращает историю карточек "Идея дня" — для аккумулированной ленты идей, чтобы оператор видел что уже предлагалось и не дублировал. Если siteId  |

## Deploy

Mount: `/api/deploys` (from `server/routes/deploy.js`)

| Method | Path | Description |
|---|---|---|
| `POST` | `/api/deploys/` | POST /api/deploy — запустить деплой (Фаза 1: имитация шагов в памяти) |
| `GET` | `/api/deploys/` | GET /api/deploys |
| `GET` | `/api/deploys/:id/status` | GET /api/deploys/:id/status |

## Imported

Mount: `/api` (from `server/routes/imported.js`)

| Method | Path | Description |
|---|---|---|
| `POST` | `/api/imported` | POST /api/imported Body: { url, purpose?, user_tags?, refetch_interval_days? } |
| `GET` | `/api/imported` | GET /api/imported?q=&purpose=&domain=&status=&limit=&offset= |
| `GET` | `/api/imported/:id` | GET /api/imported/:id |
| `DELETE` | `/api/imported/:id` | DELETE /api/imported/:id — soft archive |

## Log

Mount: `/api/log` (from `server/routes/log.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/log/` | GET /api/log?siteId=...&limit=50&offset=0 |

## Metrics

Mount: `/api/metrics` (from `server/routes/metrics.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/metrics/` | Фаза 3: GA4 / GSC. Пока — заглушка. |

## Plan

Mount: `/api` (from `server/routes/plan.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/sites/:siteId/plan` | GET /api/sites/:siteId/plan |
| `POST` | `/api/sites/:siteId/plan` | POST /api/sites/:siteId/plan |
| `PUT` | `/api/plan/:id` | PUT /api/plan/:id |
| `DELETE` | `/api/plan/:id` | DELETE /api/plan/:id |
| `POST` | `/api/plan/:id/generate-brief` | POST /api/plan/:id/generate-brief Генерит AI-бриф для плана, сохраняет в ai_brief, логирует в ai_log |
| `GET` | `/api/sites/:siteId/progress` | GET /api/sites/:siteId/progress Сводка по рубрикам и фазам: сколько в плане, сколько опубликовано, drafts, ideas |

## Portfolio

Mount: `/api/portfolio` (from `server/routes/portfolio.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/portfolio/valuation` | GET /api/portfolio/valuation  Сумма последних оценок всех активных сайтов + дельты за 24h и 30d. Используется виджетом «Live Portfolio Value» в шапке (Phase A гамификации).  Без записи в БД, без AI-вы |

## Prefs

Mount: `/api/prefs` (from `server/routes/prefs.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/prefs/:key` | GET /api/prefs/:key |
| `PUT` | `/api/prefs/:key` | PUT /api/prefs/:key  body: { value: any } |
| `DELETE` | `/api/prefs/:key` | DELETE /api/prefs/:key — сброс к default |

## Sites

Mount: `/api/sites` (from `server/routes/sites.js`)

| Method | Path | Description |
|---|---|---|
| `GET` | `/api/sites/` | GET /api/sites |
| `POST` | `/api/sites/` | POST /api/sites |
| `PUT` | `/api/sites/:id` | PUT /api/sites/:id |
| `DELETE` | `/api/sites/:id` | DELETE /api/sites/:id |
| `GET` | `/api/sites/:id/metrics` | GET /api/sites/:id/metrics?from=YYYY-MM-DD&to=YYYY-MM-DD&fill=1 fill=1 — заполнить пропущенные дни нулями (для непрерывного графика) |
| `GET` | `/api/sites/:id/valuations` | GET /api/sites/:id/valuations — история оценок стоимости (для графика) |
| `POST` | `/api/sites/:id/sync-metrics` | POST /api/sites/:id/sync-metrics?days=7 — pull GA4/GSC за последние N дней |
